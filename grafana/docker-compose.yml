services:
  grafana:
    image: grafana/grafana:12.0.1
    container_name: grafana
    restart: unless-stopped
    user: '1000'
    volumes:
      - './data:/var/lib/grafana'
    networks:
      - raspberry

networks:
  raspberry:
    external: true
